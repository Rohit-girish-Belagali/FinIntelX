{
  "cleanUrls": true,
  "buildCommand": "mkdir -p dist && cp -r finrobot_equity/web_app/templates/* dist/ && mkdir -p dist/static && cp -r finrobot_equity/web_app/static/* dist/static/ && mkdir -p dist/report && cp -r report/* dist/report/ && mkdir -p dist/output && cp -r output/* dist/output/",
  "outputDirectory": "dist"
}
